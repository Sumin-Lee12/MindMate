export type SearchData = {
  id: string;
  name: string;
  category: string;
  location: string;
  description?: string;
  created_at?: string;
};

export type SearchCategoryLabel = {
  label: string;
  value: string;
};
